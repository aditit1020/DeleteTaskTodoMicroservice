apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: appadi-delete-tasks-api-ingress
spec:
  ingressClassName: nginx
  rules:
    - host: delete-task-api.innervoicebyaditi.online
      http:
        paths:
          - pathType: Prefix
            path: "/"
            backend:
              service:
                name: appadi-delete-tasks-api
                port:
                  number: 80
